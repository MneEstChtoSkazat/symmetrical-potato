Drone Navigation with GPS Fallback

> Учебный проект. Реализация альтернативных методов навигации для дронов в симуляторе AirSim при потере GPS-сигнала.

📌 Описание
Проект демонстрирует:
- Имитацию потери GPS-сигнала во время полёта.

🧪 Требования
- AirSim (установленный отдельно)
- Python 3.8+
- Библиотеки: `opencv-python`, `numpy`, `airsim`

Установка зависимостей:
```bash
pip install -r requirements.txt
